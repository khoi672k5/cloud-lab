services:
  backend:
    build: ./mern-demo/server
    ports:
      - "5000:5000"
    environment:
      MONGODB_URI: ${MONGODB_URI}

  frontend:
    build: ./mern-demo/client
    ports:
      - "5173:5173"
